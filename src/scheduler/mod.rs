/// The scheduler is responsible for repeating cleanup and health actions as well as for triggering synchronizations
/// with peers which may be out of sync.

#[derive(Debug)]
pub struct Scheduler {}

impl Scheduler {
    pub fn new() -> Self {
        Self {}
    }

    pub async fn run(&self) {
        
    }
}
